# 项目索引 · 模块速查（改代码先看这里）

> 每个模块：文件路径 → 关键函数/路由 → 测试文件。**先看索引定位，再读对应文件**，避免全库搜索。
> 详细逻辑见 docs/产品设计-系统逻辑全览.md、docs/技术方案.md（听云维护）。

## 0. 模型效率提示（务必遵守）
- 定位问题：先查本索引 → 再 `read` 目标文件（有行号）→ 需要时才 grep 精确关键词；
- 改代码/跑脚本：heredoc `python3 <<EOF` 在本环境**偶发静默失败** → 一律写 `scripts/*.py` 文件再执行；
- 测试：小改动只跑相关文件（如 `pytest tests_web/test_period.py -q`），大改动才全量；
- 数据查询：本地 SQLite 用 `DATABASE_URL=sqlite:///./store_settle_live.db ./.venv/bin/python` 只读验证；
- AI 配置：本地 .env / 线上 deploy/.env（AI_MODEL=deepseek-v4-flash，非 qwen）。

## 1. 路由/页面
| 页面 | 路由 | 文件 |
|---|---|---|
| 登录/登出 | /login /logout | app/routers/auth_r.py |
| 文件上传/判定/入表 | /files /files/upload /files/{id}/finalize | app/routers/files_r.py + app/routers/settle_r.py |
| 绩效确认/申诉处理 | /confirm-admin /my/appeal /files/{id}/appeals/{aid}/resolve | app/routers/settle_r.py |
| 绩效与工资(两期发薪) | /perf /perf/daily /perf/export /perf/per-point | app/routers/settle_r.py + app/templates/perf.html |
| 月度重算 | /month /month/rebuild | app/routers/settle_r.py |
| 月度对账 | /recon /recon/upload2 /recon/{id}/* | app/routers/settle_r.py + app/templates/recon.html |
| 薪资找平 | /payroll-settle /payroll-settle/export /payroll-settle/{m}/{c}/update | app/routers/settle_r.py + app/templates/payroll_settle.html |
| 员工端 | /my/perf /my/appeal /my/password | app/routers/settle_r.py + app/routers/accounts_r.py |
| 店铺主档/员工管理 | /stores /staff-admin | app/routers/stores_r.py / accounts_r.py |
| 产品说明 | /product /product/raw | app/routers/info_r.py + app/product_doc.md |

## 2. 服务层（业务逻辑）
| 模块 | 文件 | 关键函数 |
|---|---|---|
| 绩效/工资 | app/services/perf.py | salary_for(点数,单价)、bonus_params、month_perf、sync_month_perf、company_summary、carry_map(余量)、month_per_point、set_month_per_point |
| 薪资找平 | app/services/period.py | sync_period_table(month,per_point)、period_rows、set_period_values(adjust_delta)、carry_map、half_stats_map、_current_recon |
| 对账/找平确认 | app/services/recon.py | run_task、parse_daily_records、ai_parse_daily_cols、attribute_daily、build_report、interpret_task、confirm_adjust、ai_employee_notes |
| 判定/入表 | app/services/flow.py | process_import、finalize_import、ensure_persons、judge_import、sync_store_for_import |
| 文件导入 | app/services/importer.py | upload_and_store、parse_file、ensure_staff_user |
| 员工登录名 | app/services/login_names.py | login_name_for（模型生成：中文拼音/日文罗马字） |
| AI 调用 | app/services/ai_chat.py | chat（重试2次）、configured |
| 配置 | app/config.py | Settings（env：AI_*/BONUS_GROUP/BONUS_AMOUNT/…，default_factory 读最新 env） |

## 3. 数据模型（app/models.py）
- ImportFile / RawRecord / FormalRecord / Person / PersonDailyStat
- MonthPerfRecord（月绩效物化：records/p1/p2/points/salary/per_point/settle*/diff*）
- PayrollPeriodRow（薪资找平：half1/half2_* 点数/金额/奖金/店数快照、settle/prev/diff/adjust 点+金额）
- ReconTask / ReconDataRow / ReconDayRow / ReconResult / AdjustRecord / AppealRecord / User

## 4. 关键数据流（速查）
```
发薪表：FormalRecord → PersonDailyStat(人×日) → sync_month_perf(月绩效物化) →
        sync_period_table(薪资找平快照: 两期点数/奖金/金额/店数) → /perf 两期视图读 half_map+half_stats
对账：ReconDataRow(对账文件全量) vs PersonDailyStats → ReconDayRow(问题行)+ReconResult(人月) →
      归因 → build_report(结果Excel) + interpret_task(AI分析) → sync_period_table 回填 settle/diff
找平：diff(系统参考,自动刷) → adjust(人工执行,输入框=剩余=偏差−已找平,保存增量累计) →
      上月余量=上月(diff−adjust)×上月单价 递延到下月 carry_map
权限：中间件 STAFF_ALLOWED 白名单 + 路由 role!=admin 双层；申诉归属校验
```

## 5. 测试
- tests_web/test_flow.py：核心流程+入表+对账+申诉
- tests_web/test_period.py：薪资找平/单价/递延/找平执行/奖金配置
- tests_web/test_ai_recon.py：AI 表头解析/AI分析/说明列
- tests_web/test_permissions.py：权限/越权
- tests_web/test_health.py：healthz/产品页
- tests_web/test_auth.py、test_login_names.py、test_core.py
- 用例全集见 docs/测试用例.md（观止维护：场景/接口/前端三类）

## 6. 部署
- 本地：scripts/dev_server.sh（加载 .env）；库 store_settle_live.db
- 线上（生产，2026-09 迁移）：`ssh store-prod`（root@8.216.55.230，密钥 ~/.ssh/store_prod，别名在 ~/.ssh/config）/opt/store-settle/releases/<TS>；容器 deploy-web-1/deploy-db-1；宿主 nginx 反代 127.0.0.1:3660 → https://store.visitworld.me；证书 /etc/letsencrypt（自旧机迁移，certbot 容器每日 3:00 renew）
- 旧机（已退服、数据保留可回滚）：`ssh store-old`（root@8.216.43.224）/opt/store-settle/（releases + backups + pgdata/uploads 卷）
- 迁移：alembic（本地需手动 create_all/ALTER；线上 entrypoint 自动 upgrade；sys_configs 种子含 PG `now()`，SQLite 手动建库不跑该迁移）
- 发布：rsync→cp .env→switch current→compose build/up；改数据前 pg_dump

## 7. 员工可见起始月与 H5（2026-10 交付）
- 员工可见起始月：SysConfig.staff_visible_from（/config 页可改，YYYY-MM 或空=不限制）；未配置回退 env STAFF_VISIBLE_FROM（默认 2026-10）。
  生效点：`/my/perf` 月份下拉只显示 ≥ 起始月；直链被隐藏月 → 回退可见最新月；无可见月 → 空态（month="" 提前 return，不读全量数据防泄漏）。管理员不受影响。
  服务函数：`perf.staff_visible_from(db)`；迁移 f5c6d7e8f9a0。
- H5 响应式：base.html 顶栏 ≤860px 折叠汉堡菜单（Alpine `menu`）；宽表包 `.tbl-wrap`，≤760px 首列 sticky；统计卡 auto-fit（perf/payroll/dashboard）；登录卡 width:min(360px,92vw)。
  测试：tests_web/test_permissions.py（隐藏月/回退/配置 roundtrip）、test_templates.py（汉堡菜单）。

## 8. 常用操作（data 相关）
- 清某月数据：scripts/reset_august.py（ORM）/ PG 用 SQL 事务（先删 formal→appeals→raw→persons/store_entities 引用→imports）
- 重算月：页面 /month/rebuild（正式表→统计→月绩效→薪资找平快照）
- 清 9 月等残留：SQL 按日期段删 formal/stats/mpf/payroll（保持演示库只有目标月）
- AI：本地 .env / 线上 deploy/.env（AI_MODEL=deepseek-v4-flash）
