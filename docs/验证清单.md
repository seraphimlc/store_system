# QA 验证清单（本地 · 2026-09）

> 用途：codex-agent-protocol 协作下 qa（观止）照此清单对**本地环境**做只读走查验证。
> 环境：本地服务 http://127.0.0.1:8000（`./scripts/dev_server.sh` 启动，自动加载 .env）；账号 `admin/demo123`；本地库 `store_settle_live.db`（SQLite）。
> 约束：全部只读——仅 GET/登录 POST；**不点击保存/上传/删除/生成**；DB 校验仅 ORM 只读查询。不做写操作，不改任何文件。

## A. 健康与登录
| # | 验证 | 通过标准 |
|---|---|---|
| A1 | GET /healthz | 200 |
| A2 | POST /login (admin/demo123) | 302 且 set-cookie 含 ss |
| A3 | 未登录 GET /perf | 302 → /login |
| A4 | 未登录 GET /recon | 302 → /login |

## B. 绩效与工资（/perf）
| # | 验证 | 通过标准 |
|---|---|---|
| B1 | /perf?month=2026-08&period=half1 | 200；标题含「绩效与工资 · 2026-08」；表头含 奖金(円)/总金额(円)/找平金额(円)/应该付金额(円) |
| B2 | 上半月行数 | 34 行（`/payroll-settle?month=` 链接计数 ≥34） |
| B3 | 小川逸（2188240606634082）上半月行 | 奖金 **¥45,000**、总金额 **¥303,250**、找平 ¥0、应该付 ¥303,250（行内：799店/565/234/29.3% 未达标） |
| B4 | 小川逸下半月（period=half2）行 | 奖金 **¥57,000**、总金额 **¥380,250**（823店/353/470/57.1% 达标） |
| B5 | 汇总卡 | 该期工资合计：上半月 **¥1,740,750**；下半月 **¥3,156,000**（= 6,039×250+231,000 / 10,752×250+468,000） |
| B6 | 切月份 | /perf?month=2026-07 → 200 正常渲染（回落显示可用月是预期），URL **无重复 month 参数**；重复参数（month 两次）也无 500 |
| B7 | 控件 | data-testid=perf-period、perf-per-point 存在 |
| B8 | page_state | window.__PAGE_STATE__ 含 page=perf、month=2026-08、salary_total=4896750、payroll_total=4896750、bonus_group=68、bonus_amount=3000、employees=34 |

## C. 薪资找平（/payroll-settle）
| # | 验证 | 通过标准 |
|---|---|---|
| C1 | /payroll-settle?month=2026-08 | 200；标题「薪资找平 · 2026-08」 |
| C2 | 行数 | data-testid="adjust-save-*" 计数 = **34**（page_state rows=34） |
| C3 | 汇总卡 | 奖金合计 **¥699,000**；本月对账偏差（参考）金额、分期已发 ¥4,896,750 |
| C4 | 上月修正 | = 0（7 月数据已清理，无递延） |
| C5 | page_state | 含 page=payroll_settle、month、rows=34、total.half_amt=4896750 |

## D. 月度对账（/recon）
| # | 验证 | 通过标准 |
|---|---|---|
| D1 | /recon?task_id=3 | 200；任务 done |
| D2 | 归因卡 | 差异归因（系统自动）存在；数值：一致 11054 / 仅系统 1457 / 仅对账 93 |
| D3 | AI 对账分析 | **真实正文**：含 `## 总体结论` `## 差异构成` `## 建议动作` 且含真实数字（如 2301/1364）；page_state ai_ready=true |
| D4 | 下载结果 | GET /recon/result?task_id=3 → 200 且 Content-Type xlsx（PK 头） |
| D5 | 上传表单 | data-testid=recon-upload-form / recon-upload-btn 存在 |

## E. 文件与权限
| # | 验证 | 通过标准 |
|---|---|---|
| E1 | GET /files | 200 |
| E2 | 员工越权 | 用员工账号（如 ogawa/demo123）GET /perf、/payroll-settle → 302 到 /my/perf 或 /login |
| E3 | 员工自己的页 | ogawa GET /my/perf?month=2026-08 → 200 且仅含本人（小川逸） |

## F. 数据库基准（只读 ORM 校验，DATABASE_URL=sqlite:///./store_settle_live.db）
| # | 校验 | 期望 |
|---|---|---|
| F1 | formal_records 8月 | 12,511 条、总点 16,791 |
| F2 | month_perf_records 8月 | 34 人、salary 合计 4,896,750、奖金合计 699,000 |
| F3 | payroll_period_rows 8月 | 34 行 |
| F4 | recon_tasks | 存在 8 月任务 status=done（id=3） |
| F5 | month_perf_records 7月 / formal 7月 | 0（7 月已清理） |

## 返回格式
按 verification_report（status: PASS|PASS_WITH_SCOPE|NEEDS_FIX；items 每项 result+evidence；findings；residual_risk）。FAIL 项给出可复现证据；不修改任何代码与数据。
